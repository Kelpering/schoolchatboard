/* <tr scope="row">
    <th>Name</th> (VARIABLE)
    <td>TEST </td> (VARIABLE)
</tr> */
//Add this to the file storing the chat room for each new message.

$(function() 
{
     
     $("p").click(function() {
        $(this).hide();
     });
     
});